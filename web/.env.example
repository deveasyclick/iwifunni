# Config is loaded from web/src/lib/config.ts

# Backend API base URL (overrides host + port defaults)
API_URL=http://localhost:8080

# Backend port (ignored when API_URL is set)
API_PORT=8080

# Current environment
# ENVIRONMENT=development