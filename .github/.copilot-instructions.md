This project is a multi-tenant notification system.

Key rules:
- All requests must resolve project_id via API key middleware
- Providers must be implemented using the Provider interface
- No direct HTTP calls to SendGrid/Twilio outside adapters
- Use registry for provider resolution